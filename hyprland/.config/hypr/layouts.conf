# ========================================
# GENERAL
# ========================================
general {
    gaps_in = 2
    gaps_out = 4
    border_size = 2
    col.active_border = $blue
    col.inactive_border = $surface0
    layout = master
}

# ========================================
# LAYOUTS
# ========================================
master {
    mfact = 0.7
    new_status = slave
    orientation = right
}
