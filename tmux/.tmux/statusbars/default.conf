# ==========================
# Statusbar: Default (Catppuccin modules)
# ==========================

set -g @catppuccin_flavor "mocha"
set -gu @catppuccin_status_background
set -g @catppuccin_window_status_style "rounded"
set -g @catppuccin_window_text " #W#{?window_zoomed_flag, 󰍉,}#{?#{@marked}, ●,}"
set -g @catppuccin_window_number "#I"
set -g @catppuccin_window_current_text " #W#{?window_zoomed_flag, 󰍉,}#{?#{@marked}, ●,}"
set -g @catppuccin_window_current_number "#I"
set -g @catppuccin_date_time_text " %A %d %B %H:%M"
set -g @catppuccin_status_right_separator "#[fg=#{@thm_surface_0},bg=default] "
set -g @catppuccin_directory_text "#{pane_current_path}"

run ~/.tmux/plugins/tmux/catppuccin.tmux

# Reset pane border settings that custom statusbar may have set
setw -g pane-border-status off
setw -g pane-border-format ""

# Highlight active pane border in mauve when in copy mode, restore catppuccin's value on exit
set-hook -g pane-mode-changed \
  'if -F "#{pane_in_mode}" \
    "set -wg pane-active-border-style fg=#cba6f7" \
    "set -wgF pane-active-border-style #{E:@catppuccin_pane_active_border_style}"'

# Status bar
set -g status-position top
set -g status-right-length 100
set -g status-left-length 100
set -g status-left "#{?window_zoomed_flag,#[bg=#f38ba8 fg=#1e1e2e] 󰍉 ZOOM ,}#{E:@catppuccin_status_session}"
set -ag status-left "#{E:@catppuccin_status_directory}"
set -g status-right "#{E:@catppuccin_status_application}"
set -agF status-right "#{E:@catppuccin_status_cpu}"
set -ag status-right "#{E:@catppuccin_status_date_time}"
